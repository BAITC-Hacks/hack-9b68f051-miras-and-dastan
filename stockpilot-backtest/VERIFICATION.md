# Проверка поставки — 23 сентября 2026

## Совместимость с основным проектом

Репозиторий: BAITC-Hacks/hack-9b68f051-miras-and-dastan, исходная ревизия 46d710c90c9b8d06304e0795b5a960d3cb5d56bb. Прочитаны AGENTS.md, package.json, tsconfig, ESLint, UI, типы Dataset, аналитические функции, демогенератор и тесты. .env и реальные коммерческие данные не копировались.

React/ReactDOM 19.0.0, Recharts 2.15.1, Zod 3.24.1, TypeScript 5.7.2 и React-типы согласованы с приложением. Vite 7.3.6 и Vitest 4.1.11 принадлежат отдельному модулю. Новые тесты имеют суффикс checks.ts, чтобы избежать смешивания с корневым Vitest 2.

В отдельной временной копии основного приложения, без node_modules внутри вложенного модуля:

- `next typegen`: успешно.
- Корневой `tsc --noEmit`: успешно, включая все вложенные TS/TSX модуля.
- Корневой `eslint .`: успешно.
- 6 исходных тестов аналитики + 2 интеграционных теста: 8/8 успешно под Vitest 2.1.8. Vitest запущен программно с config=false, чтобы вложенная проверочная папка не наследовала конфигурацию этого workspace.
- Прогноз и отклонение createStockPilotStrategy совпадают с analyzeDataset на каждом прошлом префиксе фактического демо Dataset. Изменение будущего спроса не меняет предыдущие решения.
- `next build --webpack`: успешно, включая временный клиентский маршрут /backtest-check с настоящими функциями analytics.
- Пример StockPilotBacktestBridge также прошёл TypeScript и ESLint в проверочной копии.

Проверочный маршрут и интеграционные тесты с прямыми импортами исходников хоста существуют только в work/ и не загружаются. Публичный модуль не импортирует файлы партнёра, он принимает функции явно. Существующие файлы основного проекта не изменены.

## Проверки модуля

- typecheck: успешно, strict + noUncheckedIndexedAccess.
- lint: успешно.
- test: 25/25 успешно (17 инвариантов движка и CSV + 8 проверок адаптера).
- build: успешно. Есть предупреждение о размере JS-бандла ~661 kB (gzip ~189 kB), не ошибка сборки.
- npm audit после согласования runtime-версий: 0 уязвимостей на момент установки.

Среда: Windows, Node.js 24.19.0. Глобальный npm отсутствовал; официальный npm распакован в собственный work/npm-cli. Команды выполнялись через этот локальный CLI. Для обычной установки достаточно стандартного npm.

Dev-оптимизатор esbuild в песочнице получает Access denied при перечислении родительской папки пользователя. Полный dev-режим в этой среде не подтверждён. Production preview работает. Конфигурация не ослабляет защиту ОС.

Ранее в браузере проверены загрузка демо, CAB-NYM-3X2.5, сравнение, срок 21→7 дней, сброс устаревших результатов, повторный расчёт и пошаговый просмотр. Нажатие CSV проверено, но API встроенного браузера не подтвердил событие download; сохранение файла не заявляется проверенным. Содержимое CSV, экранирование и защита от формул покрыты тестами. Автоматический таймер отдельным длительным прогоном не проверялся. Проверка адаптированной версии через Next подтверждена сборкой и тестами; ранний браузерный прогон не выдаётся за новый.

## Контрольный пример standalone

CAB-NYM-3X2.5, настройки по умолчанию: обе стратегии отгружают 316, теряют 163, имеют 1 неделю дефицита. Средний конечный запас 81.78125 и 8.34375; заказано 390 и 305; конечные остатки 94 и 4; в пути 25 и 30. Это демонстрационный результат, не обещание экономии.

## Изоляция

Локальный модуль: C:/Users/абзал/Documents/Codex/2026-09-23/stockpilot-backtest.

Снимок партнёрского кода для чтения: work/partner-reference; проверочная копия: work/integration-host, оба внутри собственного модуля. Папка партнёра на его компьютере не использовалась. В GitHub поставляются только новые stockpilot-backtest/*, без node_modules, work, dist, .env и Git-метаданных. Корневые package.json, lock, tsconfig, маршруты, стили и README не затрагиваются. Перечень поставляемых файлов — FILES_CREATED.txt. Автоматического merge в main нет.
