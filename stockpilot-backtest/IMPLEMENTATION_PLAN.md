# План реализации

## Подключение репозитория и поставка через PR

Исходная ревизия: BAITC-Hacks/hack-9b68f051-miras-and-dastan@46d710c90c9b8d06304e0795b5a960d3cb5d56bb. Прочитаны AGENTS.md, package.json, типы, аналитика, UI, тесты и конфигурация. Код партнёра используется только как эталон для чтения в work/partner-reference.

1. Совместить модуль с React 19.0.0, Recharts 2.15.1, Zod 3.24.1 и TypeScript 5.7.2 основного приложения.
2. Добавить адаптер к реальному Dataset и внедрение функций прогнозирования без прямого импорта lib/analytics партнёра.
3. Проверить, что вложенные исходники не ломают проверки родительского приложения; все проверки выполнять в своей копии work/.
4. Загрузить только собственные файлы в stockpilot-backtest/ в отдельной ветке и открыть PR. Не изменять существующие файлы и не сливать PR автоматически.

1. Сохранить изоляцию: весь код, зависимости и сборки только в этой папке. В исходной рабочей папке приложение партнёра, package.json и AGENTS.md не обнаружены; путь партнёра запрошен.
2. Создать строгие Zod-контракты, чистые стратегии, общую политику пополнения и недельный движок без доступа к будущему.
3. Создать воспроизводимые демоданные, независимый React-компонент и standalone-оболочку.
4. Проверить инварианты, отсутствие утечки, экспорт CSV, типы, lint и сборку; по возможности пройти браузерный сценарий.
5. Документировать формулы, ограничения и ручную интеграцию. Не подключать модуль автоматически.
